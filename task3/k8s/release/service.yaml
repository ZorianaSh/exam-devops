apiVersion: v1
kind: Service
metadata:
  name: task3-service
  namespace: release
spec:
  selector:
    app: task3-app
  ports:
    - protocol: TCP
      port: 8003
      targetPort: 5000
  type: ClusterIP
